# references:
# * http://www.objc.io/issue-6/travis-ci.html
# * https://github.com/supermarin/xcpretty#usage

osx_image: xcode8.3
language: objective-c
# cache: cocoapods
# podfile: Example/Podfile
# before_install:
# - gem install cocoapods # Since Travis is not always on latest version
# - pod install --project-directory=Example
script:
- set -o pipefail && xcodebuild test -workspace Example/MCAssertReflectiveEqual.xcworkspace -scheme MCAssertReflectiveEqual_Tests -sdk iphonesimulator10.3 -destination "platform=iOS Simulator,id=574DAFE4-A959-4ACD-8D60-511188465805,OS=10.3" ONLY_ACTIVE_ARCH=NO | xcpretty
- pod lib lint
