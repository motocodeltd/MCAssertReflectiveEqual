# references:
# * http://www.objc.io/issue-6/travis-ci.html
# * https://github.com/supermarin/xcpretty#usage

osx_image: xcode8.3
language: objective-c
# cache: cocoapods
# podfile: Example/Podfile
# before_install:
# - gem install cocoapods # Since Travis is not always on latest version
# - pod install --project-directory=Example
script:
- set -o pipefail && xcodebuild test -workspace Example/MCAssertReflectiveEqual.xcworkspace -scheme MCAssertReflectiveEqual_Tests -sdk iphonesimulator10.3 -destination "platform=iOS Simulator,id=85AF1F51-F2EB-4A4D-930B-0E5761511A45,OS=10.3.1" ONLY_ACTIVE_ARCH=NO | xcpretty
- pod lib lint
